[core]
        autocrlf = false
        ignorecase = false
        quotepath = false
        editor = vi
[pull]
        rebase = false
[color]
        ui = auto
[include]
        path = .gitconfig.local
# 以下の設定を`.gitconfig.local`に書いてください
# [user]
#         name = name
#         email = email